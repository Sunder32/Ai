# DeepSeek AI Project

Локальный AI-ассистент с самообучением, RAG и веб-интерфейсом.

## 🚀 Быстрая установка

### Windows:
```batch
install.bat
```

### Ручная установка:

1. **Установите Python 3.10+**
   - https://www.python.org/downloads/
   - ✅ Отметьте "Add Python to PATH"

2. **Установите Ollama**
   - https://ollama.com/download

3. **Установите зависимости**
   ```bash
   pip install -r requirements.txt
   ```

4. **Скачайте модель**
   ```bash
   ollama pull deepseek-r1:8b
   ```

5. **Создайте проектную модель**
   ```bash
   ollama create deepseek-project-model -f Modelfile
   ```

6. **Запустите сервер**
   ```bash
   start_server.bat
   ```

## 📁 Структура проекта

```
aipc/
├── server/
│   ├── main.py            # FastAPI backend
│   ├── rag_engine.py      # RAG поиск по документам
│   ├── learning_engine.py # Самообучение
│   └── finetune_prepare.py# Подготовка fine-tuning
├── client/
│   ├── index.html         # Веб-интерфейс
│   ├── app.js             # Frontend логика
│   └── style.css          # Стили
├── uploads/               # Загруженные документы
├── learning_data/         # Данные обучения
├── ollama_data/           # Модели Ollama
├── requirements.txt       # Python зависимости
├── Modelfile             # Конфиг модели
├── install.bat           # Установщик
└── start_server.bat      # Запуск сервера
```

## 🎯 Возможности

### 💬 Чат
- Общение с AI на базе DeepSeek-R1
- История чатов сохраняется локально
- Markdown рендеринг ответов

### 📚 RAG Chat
- Загрузка документов (PDF, DOCX, TXT, JSON)
- Поиск по документам при ответе
- Контекстные ответы на основе ваших данных

### 🧠 Self-Learning
- Лайкайте хорошие ответы 👍
- Исправляйте неточные ответы ✏️
- AI учится на ваших правках
- Экспорт данных для fine-tuning

### 🎓 Fine-tuning
- Подготовка датасетов
- Few-shot обучение
- Создание кастомных моделей

## 🔧 API Endpoints

| Метод | URL | Описание |
|-------|-----|----------|
| POST | `/api/chat` | Отправить сообщение |
| POST | `/api/upload` | Загрузить документ |
| POST | `/api/train` | Обучить на документах |
| POST | `/api/chat/rag` | RAG чат |
| POST | `/api/learning/correct` | Исправить ответ |
| POST | `/api/learning/like` | Лайкнуть ответ |
| GET | `/api/learning/stats` | Статистика обучения |

## 💻 Системные требования

- **ОС**: Windows 10/11, Linux, macOS
- **RAM**: 16 GB минимум (рекомендуется 32 GB)
- **GPU**: NVIDIA с 8+ GB VRAM (опционально, ускоряет в 5-10 раз)
- **Диск**: 15 GB свободного места
- **Python**: 3.10+

## 🔄 Перенос на другой компьютер

1. Скопируйте всю папку проекта
2. На новом ПК запустите `install.bat`
3. Модели скачаются автоматически

Или для переноса с моделью:
```batch
:: Скопировать папку ollama_data вместе с проектом
xcopy ollama_data %USERPROFILE%\.ollama /E /I
```

## 📝 Лицензия

MIT License
