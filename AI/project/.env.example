# Django Settings
SECRET_KEY=your-secret-key-here-generate-new-one-for-production
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,yourdomain.com

# Database (для продакшена замените на PostgreSQL)
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=db.sqlite3
# Для PostgreSQL раскомментируйте:
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=pc_configurator
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password
# DB_HOST=localhost
# DB_PORT=5432

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://127.0.0.1:3000

# Email Settings (опционально)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@example.com
EMAIL_HOST_PASSWORD=your-app-password

# Redis для кэширования (опционально)
REDIS_URL=redis://localhost:6379/0

# AI Settings (если используете Ollama)
OLLAMA_HOST=http://localhost:11434
AI_MODEL_NAME=deepseek-project-model:latest
