# Django Settings
SECRET_KEY=your-secret-key-here-generate-new-one-for-production
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,yourdomain.com

# Database (MySQL recommended for production)
DB_BACKEND=mysql
DB_NAME=pckonfai
DB_USER=root
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=3306

# For SQLite (development only):
# DB_BACKEND=sqlite

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://127.0.0.1:3000

# Email Settings (for notifications)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@example.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=PC Configurator <noreply@pckonfai.ru>

# Celery Settings (Redis required for async tasks)
CELERY_BROKER_URL=redis://localhost:6379/0

# AI Settings
AI_SERVER_URL=http://localhost:5050
OLLAMA_HOST=http://localhost:11434
AI_MODEL_NAME=deepseek-project-model:latest
